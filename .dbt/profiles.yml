eth:
  outputs:
    ci:
      account: YOUR-SNOWFLAKE-APP-IDENTIFIER #use your account iddentifier
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: your_user
      warehouse: compute_wh
  target: ci